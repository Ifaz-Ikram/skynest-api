#!/usr/bin/env bash
set -euo pipefail

# simple pre-commit: run lint, block on errors
if ! command -v npm >/dev/null 2>&1; then
  echo "pre-commit: npm not found; skipping lint" >&2
  exit 0
fi

echo "pre-commit: running npm run lint"
npm run --silent lint
